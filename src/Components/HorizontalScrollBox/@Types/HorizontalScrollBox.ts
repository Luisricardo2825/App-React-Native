import React from 'react';
import {StyleProp, ViewStyle} from 'react-native';

export interface HorizontalScrollBoxProps {
  style?: StyleProp<ViewStyle>;
  children?: React.ReactNode | React.ReactNode[] | null;
}
